from astree import AstNode

class Rule:

    def __init__(self, pattern: AstNode, replacement: AstNode):
        self.pattern = pattern
        self.replacement = replacement

    @staticmethod
    def match(tree1: AstNode, tree1: AstNode) -> bool:
        


    def apply(self, ):
        
